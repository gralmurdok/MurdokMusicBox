const ApproveButton = ({ onClickApprove }) => {
    return (
      <div className="approve_button">
        <button onClick={onClickApprove}>Aprobar</button>
      </div>
    );
  };
  
  export { ApproveButton };
  